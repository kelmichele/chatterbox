<% @recipient = conv_recipient(conversation) %>
<li class="conversation-window"
    id="<%= conversation.id %>">
    <!-- data-conversation-user-name="<%= @recipient.name %>" data-turbolinks-permanent -->

  <div class="panel panel-default" data-conversation-id="<%= conversation.id %>">
    <div class="panel-heading">
      <%= link_to conversation.opposed_user(user).name, '', class: 'toggle-window', remote: true, method: :post %>

      <%= link_to "X", close_conversation_path(conversation), class: "btn btn-xs pull-right", remote: true, method: :post %>
    </div>

    <div class="panel-body" style="display: none;">
      <div class="messages-list">
        <ul>
          <%= render 'conversations/conversation_content', messages: conversation.messages, user: user %>
        </ul>
      </div>

      <form class="new_message">
        <input name="conversation_id" type="hidden" value="<%= conversation.id %>">
        <input name="user_id" type="hidden" value="<%= user.id %>">
        <textarea name="body" class="form-control"></textarea>
        <input type="submit" class="btn btn-success" value="Send">
      </form>
    </div>
  </div>
</li>

<!-- <span class="contact-name-notif"><%= @recipient.name %></span> -->
